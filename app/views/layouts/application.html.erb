<!DOCTYPE html>
<html>
<head>
  <title>MyApp</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
</head>

  <body>
    <div id="app">
        <%= turbo_mount("Header", props: { userId: current_user&.id }) %>
       <%# <%= turbo_frame_tag "main" do %>
        <%= yield %>
      <%# <%= end %>
    </div>
  </body>
</html>
