<% if current_user.following?(@user) %>
  <%= button_to 'Unfollow', follow_path(id: @user.id), method: :delete, remote: true, id: 'follow_button' %>
<% else %>
  <%= button_to 'Follow', follows_path(followed_id: @user.id), method: :post, remote: true, id: 'follow_button' %>
<% end %>
